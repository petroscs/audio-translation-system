name: translator_app
description: Flutter translator app for audio translation system.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: any
  flutter_webrtc: ^1.0.0
  http: any
  provider: any
  qr_flutter: any
  signalr_core: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
  assets:
    - assets/
