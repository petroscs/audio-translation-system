# Backend API base URL (no trailing slash).
# When empty, the app uses relative URLs (works with Vite dev proxy or same-origin deployment).
# For a production build pointing at a specific backend, set for example:
# VITE_API_URL=http://localhost:5000
VITE_API_URL=
